# PostgreSQL pooled connection used by the running NestJS application.
DATABASE_URL=postgresql://postgres.project-ref:password@host:6543/postgres?pgbouncer=true

# Direct PostgreSQL connection used by Prisma migrations.
DIRECT_URL=postgresql://postgres.project-ref:password@host:5432/postgres

# Supabase JWT verification secret used by the current Passport strategy.
# M4 will replace this shared secret with Supabase JWKS verification.
SUPABASE_JWT_SECRET=replace-with-your-supabase-jwt-secret

# Comma-separated frontend origins allowed to call this API from a browser.
# Add the production Vercel URL and any explicitly trusted preview URL.
ALLOWED_ORIGINS=http://localhost:3000,https://ielts-simulator.vercel.app

# Local API port. Deployment platforms may inject this automatically.
PORT=3001

# Variables used only by the optional upload-media.ts maintenance script.
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your-publishable-key
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
