{
  "name": "ielts-computer-simulator",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo command hub for the IELTS Computer Simulator frontend and backend.",
  "engines": {
    "node": ">=22.15.0 <23",
    "npm": ">=10.9.2 <11"
  },
  "packageManager": "npm@10.9.2",
  "scripts": {
    "install:all": "npm ci --prefix frontend && npm ci --prefix backend",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "npm --prefix backend run start:dev",
    "build": "npm run build:backend && npm run build:frontend",
    "build:frontend": "npm --prefix frontend run build",
    "build:backend": "npm --prefix backend run build",
    "lint": "npm run lint:backend && npm run lint:frontend",
    "lint:frontend": "npm --prefix frontend run lint",
    "lint:backend": "npm --prefix backend run lint:check",
    "typecheck": "npm run typecheck:backend && npm run typecheck:frontend",
    "typecheck:frontend": "npm --prefix frontend run typecheck",
    "typecheck:backend": "npm --prefix backend run typecheck",
    "test": "npm --prefix backend run test -- --runInBand",
    "test:e2e": "npm --prefix backend run test:e2e -- --runInBand"
  }
}
